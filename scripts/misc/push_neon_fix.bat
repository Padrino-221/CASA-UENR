git config user.name "Syntax-Samurai"
git config user.email "166333956+Padrino-221@users.noreply.github.com"
git add .
git commit -m "fix(prisma): pass url explicitly into createClient() to prevent bundler scope loss"
git push origin master
